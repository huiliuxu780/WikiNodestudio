export type ParserProfile = {
  parserProfile: string
  displayName: string
  supportedRawMaterialTypes: string[]
  supportedSourceTypes: string[]
  contentFormat: string
  enabled: boolean
  version: string
}
