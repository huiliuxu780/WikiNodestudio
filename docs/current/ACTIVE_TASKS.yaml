version: 2
updated_at: "2026-06-29"
source_of_truth: "current_active_tasks"
notes:
  - "Each IM is a product capability slice and must stop after local verification and commit."
  - "Do not auto-enter the next IM without explicit user confirmation."
  - "Done history stays in release docs or archive."
tasks:
  - id: IM075
    story_id: US075
    title: "Knowledge Base Administration"
    status: in_progress
    required_workflow: default
    allowed_files:
      - "src/main/java/com/wikinode/studio/api/**"
      - "src/main/java/com/wikinode/studio/model/**"
      - "src/main/java/com/wikinode/studio/repository/**"
      - "src/main/resources/db/migration/**"
      - "src/pages/**"
      - "src/services/**"
      - "src/types/**"
      - "src/data/**"
      - "src/utils/**"
      - "src/test/java/**"
      - "tests/e2e/**"
      - "scripts/api-smoke.sh"
      - "docs/current/**"
      - "docs/registry/**"
    forbidden_files:
      - "package.json"
      - "pnpm-lock.yaml"
    acceptance:
      - "Knowledge Base list/detail/create/update/disable/archive/restore APIs are covered by backend contract tests."
      - "A database migration creates knowledge_bases and binds wiki_nodes/source_items with knowledge_base_id."
      - "WikiNode and Source API responses expose knowledgeBaseId."
      - "Retrieval API filters by knowledgeBaseId and still returns WikiNode-centered results."
      - "/knowledge-bases, /knowledge-bases/:kbId, and /knowledge-bases/:kbId/settings are API-backed dense management pages with Playwright coverage."
      - "No full RBAC, permission enforcement, approval, copy/move/delete, external sync, embedding, vector sync, Agent, Chatbot, Workflow, Vector DB Management, or product-facing Chunk Management is added."
