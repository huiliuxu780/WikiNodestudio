version: 2
updated_at: "2026-06-26"
source_of_truth: "current_active_tasks"
notes:
  - "Each IM is a product capability slice and must stop after local verification and commit."
  - "Do not auto-enter the next IM without explicit user confirmation."
  - "Done history stays in release docs or archive."
tasks:
  - id: IM055
    title: "Quality Governance Surfaces Baseline"
    status: ready
    story_ids: [US055]
    allowed_files:
      - "src/pages/**"
      - "src/components/quality/**"
      - "src/utils/display-labels.ts"
      - "tests/e2e/**"
      - "docs/current/**"
      - "docs/registry/**"
      - "docs/release/**"
    forbidden_files:
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/**"
      - "src/main/java/**"
      - "src/main/resources/db/migration/**"
    gates:
      - "TDD gate: write or update Playwright coverage for Conflicts, Expired Knowledge, and Duplicates before implementation."
      - "Product gate: keep quality governance WikiNode-centered and present conflict, expiration, duplicate, WikiLink, source evidence, Index Segment, and retrieval signals as evidence."
      - "Boundary gate: no automated conflict detection, expiration job, duplicate-matching algorithm, resolved or ignore write action, repair action, batch operation, export, embedding, external vector-store work, Source import, parser execution, permissions, publishing, or new dependencies."
      - "Verification gate: pnpm lint, pnpm build, bash scripts/check.sh, focused Playwright, and diff check."
    stop_conditions:
      - "Backend API changes, DB migration, automated quality execution, resolved or ignore write action, repair action, batch operation, export, external vector DB integration, embedding invocation, permissions, publishing approval, package or lockfile change."
