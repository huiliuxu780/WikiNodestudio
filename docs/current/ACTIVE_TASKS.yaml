version: 2
updated_at: "2026-06-27"
source_of_truth: "current_active_tasks"
notes:
  - "Each IM is a product capability slice and must stop after local verification and commit."
  - "Do not auto-enter the next IM without explicit user confirmation."
  - "Done history stays in release docs or archive."
tasks:
  - id: IM061
    title: "WikiGraph Relation Filter and Legend Completion"
    status: in_progress
    story_ids: [US061]
    allowed_files:
      - "src/components/graph/wiki-graph-view.tsx"
      - "src/components/graph/graph-inspector.tsx"
      - "src/utils/knowledge-graph.ts"
      - "tests/e2e/wiki-graph-canvas.spec.ts"
      - "tests/e2e/wiki-node-api-fixtures.ts"
      - "docs/current/STORY_QUEUE.yaml"
      - "docs/current/ACTIVE_TASKS.yaml"
      - "docs/current/PROJECT_CONTEXT.md"
      - "docs/current/BLOCKERS.md"
      - "docs/registry/DECISION_INDEX.yaml"
      - "docs/registry/TRACE_INDEX.yaml"
    forbidden_files:
      - "src/main/**"
      - "src/services/**"
      - "src/data/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - ".github/workflows/**"
    gates:
      - "Relation filter gate: support relation type and relation status filtering on the existing React Flow WikiGraph."
      - "Visual semantics gate: distinguish normal, conflict, and broken relation edges and expose a legend."
      - "Edge detail gate: clicking a graph edge opens relation detail in the Inspector."
      - "No graph rewrite gate: do not add dependencies, layout algorithms, backend graph APIs, or database changes."
      - "Verification gate: focused Playwright, pnpm lint, pnpm build, bash scripts/check-state.sh, bash scripts/check.sh, full Playwright, and diff check."
    stop_conditions:
      - "Any backend API, Java model, database migration, new dependency, package or lockfile change, graph database, graph layout algorithm, approval lifecycle, confirm or reject operation, batch operation, repair action, AI suggestion, permission flow, retrieval behavior, parser execution, vector sync, or Source import."
