version: 2
updated_at: "2026-06-30"
source_of_truth: "current_active_tasks"
notes:
  - "Each IM is a product capability slice and must stop after local verification and commit."
  - "Do not auto-enter the next IM without explicit user confirmation."
  - "Done history stays in release docs or archive."
tasks:
  - id: IM079
    story_id: US077
    title: "Imported Knowledge Live Acceptance Loop"
    status: in_progress
    required_workflow: default
    allowed_files:
      - "src/main/java/com/wikinode/studio/api/**"
      - "src/main/java/com/wikinode/studio/model/**"
      - "src/main/java/com/wikinode/studio/repository/**"
      - "src/main/resources/db/migration/**"
      - "src/pages/**"
      - "src/services/**"
      - "src/types/**"
      - "src/data/**"
      - "src/utils/**"
      - "src/test/java/**"
      - "tests/e2e/**"
      - "scripts/api-smoke.sh"
      - "docs/current/**"
      - "docs/registry/**"
    forbidden_files:
      - "package.json"
      - "pnpm-lock.yaml"
    acceptance:
      - "Import success gives a clear primary route into the generated Draft WikiNode Suggestion review."
      - "Draft WikiNode Suggestion detail exposes source evidence, suggested WikiNode fields, metadata, sourceRefs, relations, and Knowledge Base ownership in a dense operator-facing review layout."
      - "Accepting the suggestion creates or updates a WikiNode and preserves knowledgeBaseId, sourceId, rawMaterialId, parsedDocumentId, sourceRefs, objectType, subtype, metadata, and relations."
      - "Accepted WikiNode prepares local Index Segment evidence and remains visible from WikiNode detail."
      - "WikiGraph shows the accepted WikiNode and imported relation evidence under Knowledge Base scope."
      - "Retrieval Test returns the accepted WikiNode for a scoped Knowledge Base query."
      - "A no-mock live Playwright acceptance path covers import, suggestion review, draft WikiNode, publish, WikiGraph, and Retrieval Test."
      - "No real external connector, embedding, external vector sync, approval workflow, RBAC, batch operation, export, Agent, Chatbot, Workflow, Vector DB Management, product-facing Chunk Management, package change, or lockfile change is added."
