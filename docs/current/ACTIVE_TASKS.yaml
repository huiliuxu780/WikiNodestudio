version: 2
updated_at: "2026-06-28"
source_of_truth: "current_active_tasks"
notes:
  - "Each IM is a product capability slice and must stop after local verification and commit."
  - "Do not auto-enter the next IM without explicit user confirmation."
  - "Done history stays in release docs or archive."
tasks:
  - id: IM072
    story_id: US072
    title: "Publishing and Index Lifecycle Local Execution"
    status: in_progress
    required_workflow: default
    allowed_files:
      - "src/main/java/com/wikinode/studio/api/**"
      - "src/main/java/com/wikinode/studio/model/**"
      - "src/main/java/com/wikinode/studio/repository/**"
      - "src/main/resources/db/migration/**"
      - "src/services/**"
      - "src/types/**"
      - "src/pages/**"
      - "src/utils/display-labels.ts"
      - "tests/e2e/**"
      - "src/test/java/**"
      - "scripts/api-smoke.sh"
      - "docs/current/**"
      - "docs/registry/**"
    forbidden_files:
      - "package.json"
      - "pnpm-lock.yaml"
    acceptance:
      - "POST /api/wiki-nodes/{nodeId}/publish updates local WikiNode lifecycle state and prepares Index Segments."
      - "POST /api/wiki-nodes/{nodeId}/reindex regenerates local Index Segments without external vector sync."
      - "WikiNode editor publish/reindex actions call backend APIs and show clear Chinese feedback."
      - "No embedding, external vector write/sync, connector, auth, permission, approval, batch, export, Agent, Chatbot, Workflow, or Vector DB Management is added."
