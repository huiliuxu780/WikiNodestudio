version: 2
updated_at: "2026-06-28"
source_of_truth: "current_active_tasks"
notes:
  - "Each IM is a product capability slice and must stop after local verification and commit."
  - "Do not auto-enter the next IM without explicit user confirmation."
  - "Done history stays in release docs or archive."
tasks:
  - id: IM071
    story_id: US071
    title: "Draft WikiNode Index Segment Preparation"
    status: in_progress
    required_workflow: default
    allowed_files:
      - "src/main/java/com/wikinode/studio/api/**"
      - "src/main/java/com/wikinode/studio/model/**"
      - "src/main/java/com/wikinode/studio/repository/**"
      - "src/main/resources/db/migration/**"
      - "src/services/**"
      - "src/types/**"
      - "src/pages/**"
      - "src/utils/display-labels.ts"
      - "tests/e2e/**"
      - "src/test/java/**"
      - "scripts/api-smoke.sh"
      - "docs/current/**"
      - "docs/registry/**"
    forbidden_files:
      - "package.json"
      - "pnpm-lock.yaml"
    acceptance:
      - "POST /api/draft-wikinode-suggestions/{suggestionId}/accept creates a draft WikiNode and prepares local Index Segments for the accepted draft."
      - "The accept result returns nodeId, nodeStatus, and indexSegmentCount."
      - "GET /api/wiki-nodes/{nodeId}/index-segments returns prepared not_indexed Index Segments for the accepted draft WikiNode."
      - "No publish, index execution, embedding, vector sync, external connector, auth, permission, batch, export, Agent, Chatbot, Workflow, or Vector DB Management is added."
