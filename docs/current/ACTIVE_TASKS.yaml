version: 2
updated_at: "2026-06-29"
source_of_truth: "current_active_tasks"
notes:
  - "Each IM is a product capability slice and must stop after local verification and commit."
  - "Do not auto-enter the next IM without explicit user confirmation."
  - "Done history stays in release docs or archive."
tasks:
  - id: IM076
    story_id: US076
    title: "Source Import to Knowledge Base Execution"
    status: in_progress
    required_workflow: default
    allowed_files:
      - "src/main/java/com/wikinode/studio/api/**"
      - "src/main/java/com/wikinode/studio/model/**"
      - "src/main/java/com/wikinode/studio/repository/**"
      - "src/main/resources/db/migration/**"
      - "src/pages/**"
      - "src/services/**"
      - "src/types/**"
      - "src/data/**"
      - "src/utils/**"
      - "src/test/java/**"
      - "tests/e2e/**"
      - "scripts/api-smoke.sh"
      - "docs/current/**"
      - "docs/registry/**"
    forbidden_files:
      - "package.json"
      - "pnpm-lock.yaml"
    acceptance:
      - "A local Source import execution API accepts source-scoped content and records the run under a Knowledge Base."
      - "Imported content creates or updates Raw Material, Parsed Document, and document segment evidence with knowledgeBaseId ownership preserved."
      - "The existing Draft WikiNode Suggestion and local Index Segment preparation path can consume the imported Parsed Document evidence."
      - "Source, Raw Material, Parsed Document, Knowledge Base detail, WikiGraph, and Retrieval Test surfaces expose the same ownership chain in dense operator-facing layouts."
      - "Backend contract tests, API smoke, and Playwright cover the Source import to Knowledge Base evidence chain."
      - "No real external connector, embedding, external vector sync, approval workflow, RBAC, batch operation, export, Agent, Chatbot, Workflow, Vector DB Management, product-facing Chunk Management, package change, or lockfile change is added."
