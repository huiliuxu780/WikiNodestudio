version: 2
updated_at: "2026-06-29"
source_of_truth: "current_active_tasks"
notes:
  - "Each IM is a product capability slice and must stop after local verification and commit."
  - "Do not auto-enter the next IM without explicit user confirmation."
  - "Done history stays in release docs or archive."
tasks:
  - id: IM074
    story_id: US074
    title: "Manual E2E Acceptance Pack"
    status: in_progress
    required_workflow: default
    allowed_files:
      - "docs/current/**"
      - "docs/registry/**"
    forbidden_files:
      - "package.json"
      - "pnpm-lock.yaml"
    acceptance:
      - "Add a manual acceptance pack for Source import through WikiGraph and Retrieval API."
      - "List preconditions, sample markdown content, browser steps, API guard, pass criteria, fail criteria, and explicit non-goals."
      - "Reference existing API smoke guard instead of adding product behavior."
      - "Do not change product code, backend API, DB migration, package files, dependency, external vector sync, Agent, Chatbot, Workflow, or Vector DB Management scope."
