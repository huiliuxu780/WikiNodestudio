version: 2
updated_at: "2026-07-01"
source_of_truth: "current_active_tasks"
notes:
  - "Each IM is a product capability slice and must stop after local verification and commit."
  - "Do not auto-enter the next IM without explicit user confirmation."
  - "Done history stays in release docs or archive."
tasks:
  - id: IM081
    story_id: US081
    title: "Source Connector Credential UX and Connection Readiness"
    status: in_progress
    required_workflow: default
    allowed_files:
      - "src/main/java/com/wikinode/studio/api/**"
      - "src/main/java/com/wikinode/studio/model/**"
      - "src/main/java/com/wikinode/studio/repository/**"
      - "src/main/resources/db/migration/**"
      - "src/pages/**"
      - "src/services/**"
      - "src/types/**"
      - "src/data/**"
      - "src/utils/**"
      - "src/test/java/**"
      - "tests/e2e/**"
      - "scripts/api-smoke.sh"
      - "docs/current/**"
      - "docs/registry/**"
    forbidden_files:
      - "package.json"
      - "pnpm-lock.yaml"
    acceptance:
      - "Sources List shows credential status and last credential check in the dense Source operations table."
      - "Source Detail header summary shows credential readiness without exposing secret values."
      - "Source Detail Ingestion Configuration tab separates Connector Profile from Credential and Connection Readiness."
      - "Knowledge Base Detail Source tab shows credential readiness in the ownership table and keeps Source Detail / Import File actions."
      - "Processing Records show connection-check evidence through Source Operation rows."
      - "No real external connector, OAuth, secret storage, token/password/private-key input, external API/database/web crawler execution, external scheduling, embedding, external vector sync, approval workflow, RBAC, batch operation, export, Agent, Chatbot, Workflow, Vector DB Management, product-facing Chunk Management, package change, or lockfile change is added."
